"use client";

import { ApplyDoneScreen } from "@/src/screens/ApplyDoneScreen";

export default function SubsidyDone() {
  // ponytail: 문단 재생성은 아직 무동작. Claude API 붙이면 onRegenerate 에 호출만 넣으면 된다.
  return <ApplyDoneScreen />;
}
